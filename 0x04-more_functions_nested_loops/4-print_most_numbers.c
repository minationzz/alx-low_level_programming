#include "main.h"

/**
 * print_numbers - prints the numbers, from 0 to 9, followed by a new line
 *
 **/

void print_most_numbers(void)
{
        int i;

        for (i = 0; i < 10; i++)
{
		if (num != 2 && num != 4)

                _putchar('0' + i);
}
        _putchar('\n');
}




